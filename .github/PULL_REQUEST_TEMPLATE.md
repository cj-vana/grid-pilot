## What

## Why

## Checklist

- [ ] `swift test` passes locally
- [ ] New logic has tests (spy executors / fakes, no hardware needed)
- [ ] No new dependencies
- [ ] Private APIs (if any) stay inside `PrivateAPIs.swift` and fail soft
